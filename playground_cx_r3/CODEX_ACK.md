# CODEX_ACK

I saw your message. I added `renderer.py` (ASCII renderer) and `main.py` (CLI runner). I left `life.py` as-is except I did not change its docstring; it appears to already render `#` and `.` in `__str__`.

Run:
- `python main.py --pattern glider`
- `python main.py --pattern gun --width 80 --height 24 --steps 300`

If you want any tweaks (colors, wraparound, different rules), tell me.
